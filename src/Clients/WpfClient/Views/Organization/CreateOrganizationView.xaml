<UserControl x:Class="WpfClient.Views.Organization.CreateOrganizationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             xmlns:local="clr-namespace:WpfClient.Views.Organization"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">

    <GroupBox Header="Create New Organization" Padding="15">
        <StackPanel>
            <ui:TextBlock Text="Organization Name"/>
            <ui:TextBox Text="{Binding OrganizationName, UpdateSourceTrigger=PropertyChanged}" Margin="0,5,0,10"/>

            <ui:TextBlock Text="Abbreviation"/>
            <ui:TextBox Text="{Binding OrganizationAbbreviation, UpdateSourceTrigger=PropertyChanged}" Margin="0,5,0,20"/>

            <ui:Button Content="Create Organization" 
                       Command="{Binding CreateOrganizationCommand}" 
                       HorizontalAlignment="Left"/>
        </StackPanel>
    </GroupBox>
</UserControl>
