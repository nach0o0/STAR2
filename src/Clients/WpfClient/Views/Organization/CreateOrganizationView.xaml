<UserControl x:Class="WpfClient.Views.Organization.CreateOrganizationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             xmlns:local="clr-namespace:WpfClient.Views.Organization"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">

    <GroupBox Header="Create New Top-Level Organization" Padding="15">
        <StackPanel>
            <ui:TextBlock Text="Organization Name"/>
            <ui:TextBox Text="{Binding NewOrganizationName, UpdateSourceTrigger=PropertyChanged}" Margin="0,5,0,10"/>

            <TextBlock Text="Abbreviation"/>
            <ui:TextBox Text="{Binding NewOrganizationAbbreviation, UpdateSourceTrigger=PropertyChanged}" Margin="0,5,0,20"/>

            <StackPanel Orientation="Horizontal">
                <ui:Button Content="Create Organization" Command="{Binding SubmitCommand}"/>
                <ui:Button Content="Cancel" Command="{Binding CancelCommand}" Appearance="Secondary" Margin="10,0,0,0"/>
            </StackPanel>
        </StackPanel>
    </GroupBox>
</UserControl>
