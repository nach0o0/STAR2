<UserControl x:Class="WpfClient.Views.Admin.RoleDetailsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             xmlns:local="clr-namespace:WpfClient.Views.Admin"
             xmlns:converters="clr-namespace:WpfClient.Converters"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
        <converters:InverseBooleanConverter x:Key="InverseBooleanConverter"/>
        <converters:InverseBooleanToVisibilityConverter x:Key="InverseBooleanToVisibilityConverter"/>
    </UserControl.Resources>

    <ScrollViewer VerticalScrollBarVisibility="Auto" Padding="0,0,10,0">
        <StackPanel>
            <ui:TextBlock Text="Role Details" FontSize="18" FontWeight="SemiBold"/>

            <ui:TextBlock Text="Name" Margin="0,15,0,0"/>
            <ui:TextBox Text="{Binding Role.Name, UpdateSourceTrigger=PropertyChanged}" 
                                Margin="0,5,0,10" 
                                IsReadOnly="{Binding CanEditRoleDetails, Converter={StaticResource InverseBooleanConverter}}"/>

            <ui:TextBlock Text="Description"/>
            <ui:TextBox Text="{Binding Role.Description, UpdateSourceTrigger=PropertyChanged}" 
                                Margin="0,5,0,10" Height="80" TextWrapping="Wrap" VerticalContentAlignment="Top"
                                IsReadOnly="{Binding CanEditRoleDetails, Converter={StaticResource InverseBooleanConverter}}"/>

            <ui:TextBlock Text="Permissions" FontWeight="SemiBold" Margin="0,15,0,5"/>
            <Border BorderBrush="#FFD1D1D1" BorderThickness="1" MaxHeight="200" Margin="0,0,0,10" CornerRadius="4">
                <ui:ListView ItemsSource="{Binding Role.Permissions}"/>
            </Border>

            <ui:Button Content="Manage Permissions"
                       HorizontalAlignment="Left"
                       Command="{Binding ManagePermissionsCommand}"
                       IsEnabled="{Binding CanManageRolePermissions}"/>

            <StackPanel Orientation="Horizontal" Margin="0,25,0,0">
                <ui:Button Content="Save Changes"
                           Command="{Binding SaveChangesCommand}" 
                           IsEnabled="{Binding CanEditRoleDetails}"/>
                <ui:Button Content="Delete Role" Appearance="Danger" Margin="10,0,0,0" 
                           Command="{Binding DeleteRoleCommand}"
                           Visibility="{Binding CanDeleteRoles, Converter={StaticResource BooleanToVisibilityConverter}}"/>
            </StackPanel>
        </StackPanel>
    </ScrollViewer>
</UserControl>
