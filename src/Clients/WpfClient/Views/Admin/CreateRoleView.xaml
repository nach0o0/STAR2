<UserControl x:Class="WpfClient.Views.Admin.CreateRoleView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             xmlns:local="clr-namespace:WpfClient.Views.Admin"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">

    <StackPanel>
        <ui:TextBlock Text="Create New Role" FontSize="18" FontWeight="SemiBold"/>

        <ui:TextBlock Text="Name" Margin="0,15,0,0"/>
        <ui:TextBox Text="{Binding NewRoleName, UpdateSourceTrigger=PropertyChanged}" Margin="0,5,0,10"/>

        <ui:TextBlock Text="Description"/>
        <ui:TextBox Text="{Binding NewRoleDescription, UpdateSourceTrigger=PropertyChanged}" 
                    Margin="0,5,0,10" 
                    Height="80" 
                    TextWrapping="Wrap" 
                    VerticalContentAlignment="Top"/>

        <StackPanel Orientation="Horizontal" Margin="0,15,0,0">
            <ui:Button Content="Create Role" Command="{Binding SubmitCommand}"/>
            <ui:Button Content="Cancel" Command="{Binding CancelCommand}" Appearance="Secondary" Margin="10,0,0,0"/>
        </StackPanel>
    </StackPanel>
</UserControl>
