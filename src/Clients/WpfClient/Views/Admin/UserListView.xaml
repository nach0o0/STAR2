<UserControl x:Class="WpfClient.Views.Admin.UserListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             xmlns:local="clr-namespace:WpfClient.Views.Admin"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">

    <Border Padding="10" Background="#F8F9FA" BorderBrush="#DEE2E6" BorderThickness="0,0,1,0">
        <DockPanel>
            <StackPanel DockPanel.Dock="Top" Margin="5">
                <ui:TextBlock Text="Manage Users" FontSize="18" FontWeight="SemiBold"/>

                <ui:TextBlock Text="Add user by e-mail" Margin="0,15,0,0"/>
                <Grid Margin="0,5,0,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <ui:TextBox Grid.Column="0" Text="{Binding EmailToFind, UpdateSourceTrigger=PropertyChanged}"/>
                    <ui:Button Grid.Column="1" Content="Find" Command="{Binding FindUserByEmailCommand}" Margin="5,0,0,0"/>
                </Grid>
                <Separator Margin="0,15,0,5"/>
                <ui:TextBlock Text="Relevant Users in this Scope"/>
            </StackPanel>

            <ui:ListView ItemsSource="{Binding RelevantUsers}" 
                      SelectedItem="{Binding SelectedUser, Mode=TwoWay}"
                      BorderThickness="0" 
                      Background="Transparent"
                      ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                      Margin="0,5,0,0">
                <ui:ListView.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Margin="5">
                            <ui:TextBlock Text="{Binding DisplayName, Mode=OneWay}" FontWeight="SemiBold"/>
                            <ui:TextBlock Text="{Binding Email}" FontSize="11" Foreground="Gray"/>
                        </StackPanel>
                    </DataTemplate>
                </ui:ListView.ItemTemplate>
            </ui:ListView>
        </DockPanel>
    </Border>
</UserControl>
