<UserControl x:Class="WpfClient.Views.Admin.RoleListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WpfClient.Views.Admin"
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             xmlns:converters="clr-namespace:WpfClient.Converters"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
    </UserControl.Resources>

    <Border Padding="10" Background="#F8F9FA" BorderBrush="#DEE2E6" BorderThickness="0,0,1,0">
        <DockPanel>
            <StackPanel DockPanel.Dock="Top" Margin="5">
                <ui:TextBlock Text="Roles" FontSize="18" FontWeight="SemiBold"/>
                <ui:Button Content="Create New Role" Margin="0,10"
                            Command="{Binding RequestCreateNewRoleCommand}"
                            Visibility="{Binding CanCreateRoles, Converter={StaticResource BooleanToVisibilityConverter}}"/>
            </StackPanel>

            <ui:ListView ItemsSource="{Binding Roles}" 
                             SelectedItem="{Binding SelectedRole, Mode=TwoWay}"
                             BorderThickness="0" 
                             Background="Transparent"
                             ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                <ui:ListView.ItemTemplate>
                    <DataTemplate>
                        <ui:TextBlock Text="{Binding Name}" Margin="5" ToolTip="{Binding Description}"/>
                    </DataTemplate>
                </ui:ListView.ItemTemplate>
            </ui:ListView>
        </DockPanel>
    </Border>
</UserControl>
