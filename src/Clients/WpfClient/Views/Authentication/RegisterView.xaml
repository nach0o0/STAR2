<UserControl x:Class="WpfClient.Views.Authentication.RegisterView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             xmlns:converters="clr-namespace:WpfClient.Converters"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <converters:BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter" />
    </UserControl.Resources>

    <Grid Background="{StaticResource SecondaryBackgroundBrush}">
        <Border Width="350" Padding="30" CornerRadius="8" Background="{StaticResource PrimaryBackgroundBrush}">
            <Border.Effect>
                <DropShadowEffect ShadowDepth="1" Color="Black" Opacity="0.1" BlurRadius="10"/>
            </Border.Effect>
            <StackPanel>
                <ui:TextBlock Text="Register" FontSize="24" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,0,0,20"/>

                <ui:TextBlock Text="Email"/>
                <ui:TextBox Text="{Binding Email, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Margin="0,5,0,10"/>

                <ui:TextBlock Text="Password"/>
                <ui:PasswordBox Password="{Binding Password, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Margin="0,5,0,20"/>

                <ui:TextBlock Text="Confirm Password"/>
                <ui:PasswordBox Password="{Binding ConfirmPassword, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Margin="0,5,0,20"/>

                <ui:Button Content="Register" Command="{Binding SubmitCommand}" IsDefault="True"/>

                <ui:TextBlock HorizontalAlignment="Center" Margin="0,20,0,0">
                    <Run Text="Already have an account?"/>
                    <Hyperlink Command="{Binding NavigateToLoginCommand}">
                        <Run Text="Login here"/>
                    </Hyperlink>
                </ui:TextBlock>

                <ui:ProgressRing IsIndeterminate="True" Margin="0,10,0,0"
                                 Visibility="{Binding IsLoading, Converter={StaticResource BooleanToVisibilityConverter}}"/>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
