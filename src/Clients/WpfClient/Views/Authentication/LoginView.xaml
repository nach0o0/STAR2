<UserControl x:Class="WpfClient.Views.Authentication.LoginView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             xmlns:converters="clr-namespace:WpfClient.Converters"
             xmlns:local="clr-namespace:WpfClient.Views.Authentication"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">

    <UserControl.Resources>
        <converters:BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter" />
        <converters:StringToVisibilityConverter x:Key="StringToVisibilityConverter" />
    </UserControl.Resources>
    
    <Grid Background="{StaticResource SecondaryBackgroundBrush}">
        <Border Width="350" Padding="30" CornerRadius="8" Background="{StaticResource PrimaryBackgroundBrush}">
            <Border.Effect>
                <DropShadowEffect ShadowDepth="1" Color="Black" Opacity="0.1" BlurRadius="10"/>
            </Border.Effect>
            <StackPanel>
                <TextBlock Text="Login" FontSize="24" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,0,0,20"/>

                <TextBlock Text="{Binding InfoMessage}" Foreground="Green" FontWeight="SemiBold"
                           Margin="0,0,0,10" TextWrapping="Wrap"
                           Visibility="{Binding InfoMessage, Converter={StaticResource StringToVisibilityConverter}}"/>
                
                <TextBlock Text="Email"/>
                <ui:TextBox Text="{Binding Email, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Margin="0,5,0,10"/>

                <TextBlock Text="Password"/>
                <ui:PasswordBox Password="{Binding Password, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Margin="0,5,0,20"/>

                <TextBlock Text="{Binding ErrorMessage}" Foreground="{StaticResource ErrorBrush}" 
                           Visibility="{Binding ErrorMessage, Converter={StaticResource StringToVisibilityConverter}}"
                           Margin="0,0,0,10" TextWrapping="Wrap"/>

                <ui:Button Content="Login" Command="{Binding SubmitCommand}" IsDefault="True"/>

                <TextBlock HorizontalAlignment="Center" Margin="0,20,0,0">
                    <Run Text="Don't have an account?"/>
                    <Hyperlink Command="{Binding NavigateToRegisterCommand}">
                        <Run Text="Register here"/>
                    </Hyperlink>
                </TextBlock>

                <ui:ProgressRing IsIndeterminate="True" Margin="0,10,0,0"
                                 Visibility="{Binding IsLoading, Converter={StaticResource BooleanToVisibilityConverter}}"/>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
