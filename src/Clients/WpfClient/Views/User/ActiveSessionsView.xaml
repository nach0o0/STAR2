<UserControl x:Class="WpfClient.Views.User.ActiveSessionsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WpfClient.Views.User"
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">

    <GroupBox Header="Active Sessions" Padding="15" Margin="0,20,0,0">
        <StackPanel>
            <ui:TextBlock Text="Here you can see all your active sessions. Logging out will only end the current session." TextWrapping="Wrap" Margin="0,0,0,15"/>
            <ListView ItemsSource="{Binding ActiveSessions}" BorderThickness="0" MaxHeight="200">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Margin="0,5">
                            <ui:TextBlock Text="{Binding ClientInfo}" FontWeight="SemiBold"/>
                            <ui:TextBlock Text="{Binding CreatedAt, StringFormat='Logged in on: {0:dd.MM.yyyy HH:mm}'}" Foreground="Gray"/>
                        </StackPanel>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
            <ui:Button Content="Logout from this session" Command="{Binding LogoutCommand}" 
                       Appearance="Secondary" HorizontalAlignment="Left" Margin="0,20,0,0"/>
        </StackPanel>
    </GroupBox>
</UserControl>
