<UserControl x:Class="WpfClient.Views.User.ProfileView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             xmlns:converters="clr-namespace:WpfClient.Converters"
             xmlns:helpers="clr-namespace:WpfClient.Helpers"
             xmlns:userViews="clr-namespace:WpfClient.Views.User"
             xmlns:viewModels="clr-namespace:WpfClient.ViewModels.User"
             mc:Ignorable="d" 
             d:DesignHeight="700" d:DesignWidth="800">
    <UserControl.Resources>
        <converters:StringToVisibilityConverter x:Key="StringToVisibilityConverter"/>
    </UserControl.Resources>

    <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled"
                  helpers:AutoScrollHelper.AutoScrollToTopOnChange="{Binding ErrorMessage}">
        <StackPanel Margin="30">
            <TextBlock Text="My Profile" FontSize="28" FontWeight="SemiBold" Margin="0,0,0,20"/>

            <TextBlock Text="{Binding SuccessMessage}" Foreground="Green" Margin="0,0,0,10"
                           Visibility="{Binding SuccessMessage, Converter={StaticResource StringToVisibilityConverter}}"/>
            <TextBlock Text="{Binding ErrorMessage}" Foreground="{StaticResource ErrorBrush}" Margin="0,0,0,10"
                           Visibility="{Binding ErrorMessage, Converter={StaticResource StringToVisibilityConverter}}"/>

            <userViews:ProfileInfoView DataContext="{Binding ProfileInfoViewModel}" />
            <userViews:ChangePasswordView DataContext="{Binding ChangePasswordViewModel}" />
            <userViews:ActiveSessionsView DataContext="{Binding ActiveSessionsViewModel}" />
            <userViews:DeleteAccountView DataContext="{Binding DeleteAccountViewModel}" />

        </StackPanel>
    </ScrollViewer>
</UserControl>
