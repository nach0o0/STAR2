<UserControl x:Class="WpfClient.Views.User.ProfileInfoView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             xmlns:local="clr-namespace:WpfClient.Views.User"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">

    <GroupBox Padding="15" Style="{StaticResource ProfileGroupBoxStyle}">
        <StackPanel>
            <ui:TextBlock Text="Email Address"/>
            <ui:TextBlock Text="{Binding Email}" FontWeight="SemiBold" Margin="0,5,0,15"/>

            <ui:TextBlock Text="First Name"/>
            <ui:TextBox Text="{Binding Profile.FirstName, UpdateSourceTrigger=PropertyChanged}" Margin="0,5,0,10"/>

            <ui:TextBlock Text="Last Name"/>
            <ui:TextBox Text="{Binding Profile.LastName, UpdateSourceTrigger=PropertyChanged}" Margin="0,5,0,20"/>

            <ui:Button Content="Update Profile" Command="{Binding UpdateProfileCommand}" HorizontalAlignment="Left" Style="{StaticResource UpdateProfileButtonStyle}"/>
            <ui:Button Content="Create Profile" Command="{Binding CreateProfileCommand}" HorizontalAlignment="Left" Style="{StaticResource CreateProfileButtonStyle}"/>
        </StackPanel>
    </GroupBox>
</UserControl>
